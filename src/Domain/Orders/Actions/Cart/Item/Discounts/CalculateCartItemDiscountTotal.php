<?php

namespace Domain\Orders\Actions\Cart\Item\Discounts;

class CalculateCartItemDiscountTotal
{
    /*
     * 	function getDiscountTotal(){
            $total = 0;
            if(count($this->discounts) > 0){
                foreach($this->discounts as $d){
                    $total += $d->getTotal($this->price);
                }
            }
            return $this->discount_total = Math::formatPrice(Currency::ReverseConvert(Currency::ConvertFormatNumber($total)));
        }
     */
}
