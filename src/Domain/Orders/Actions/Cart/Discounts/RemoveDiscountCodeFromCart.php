<?php

namespace Domain\Orders\Actions\Cart\Discounts;

use Support\Contracts\AbstractAction;

class RemoveDiscountCodeFromCart
//    extends AbstractAction
{
    /*
     *
            function removeDiscountCode($code){
                $use = array();
                foreach($this->applied_codes as $c){
                    if($c != $code) $use[] = $c;
                }
                unset($this->applied_codes);
                $this->applied_codes = $use;
                unset($this->applying_code);
                $this->buildDiscounts();
                return true;
            }
     */
}
