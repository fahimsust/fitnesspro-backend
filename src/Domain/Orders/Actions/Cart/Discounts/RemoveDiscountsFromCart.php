<?php

namespace Domain\Orders\Actions\Cart\Discounts;

class RemoveDiscountsFromCart
{
    /*
     * 		function removeDiscounts(){
                if(is_array($this->items)){
                    foreach($this->items as $i){
                        if($i->free_from_discount_advantage > 0){
                            $this->deleteFromCart($i->get("id"));
                        }else $i->removeDiscounts();
                    }
                }
                unset($this->product_discounts);
                return true;
            }
     */
}
